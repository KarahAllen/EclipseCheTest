projects:
  - name: my-python-project
    source:
      type: git
      location: 'https://github.com/KarahAllen/EclipseCheTest.git' # Replace with your project's URL

components:
  - type: dockerimage
    alias: python
    image: 'quay.io/eclipse/che-python-3.6:nightly' # Or another suitable Python image
    memoryLimit: 512Mi
    mountSources: true

commands:
  - name: run-my-script
    actions:
      - type: exec
        component: python
        command: python python_test_file.py # Replace with your script's name
        workdir: '${CHE_PROJECTS_ROOT}/my-python-project' # Adjust workdir if needed
